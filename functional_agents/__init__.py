"""Functional agent orchestration layer (J5.0a).

Principle:
    Profiles = domain knowledge
    Agents   = workflow functions
    Research Objects = shared durable state

This package adds a parallel functional-agent layer on top of the existing
research_agent engine.  It does not replace or modify research_agent.
"""
